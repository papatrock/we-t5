<!DOCTYPE html>
<html>
  <head>
    <title>T5 Game Reviews</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
          <%= link_to 'Game Reviews', root_path, class: 'navbar-brand' %>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <% if user_signed_in? && current_user.admin? %>
                <li class="nav-item"><%= link_to 'Novo Jogo', new_jogo_path, class: 'nav-link' %></li>
                <li class="nav-item"><%= link_to 'Desenvolvedoras', desenvolvedoras_path, class: 'nav-link' %></li>
                <li class="nav-item"><%= link_to 'Marcadores', marcadors_path, class: 'nav-link' %></li>
              <% end %>
            </ul>
            <ul class="navbar-nav">
              <% if user_signed_in? %>
                <li class="nav-item"><%= link_to 'Editar Perfil', edit_profile_path, class: 'nav-link' %></li>
                <li class="nav-item"><%= button_to 'Sair', destroy_user_session_path, method: :delete, class: 'btn btn-outline-danger' %></li>
              <% else %>
                <li class="nav-item"><%= link_to 'Entrar', new_user_session_path, class: 'nav-link' %></li>
                <li class="nav-item"><%= link_to 'Cadastrar', new_user_registration_path, class: 'nav-link' %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>

    <main class="container mt-4">
      <% if notice %>
        <p class="alert alert-notice"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-alert"><%= alert %></p>
      <% end %>

      <%= yield %>
    </main>

    <footer class="text-center text-white-50 mt-5 mb-3">
      <p>Lorem Ipsum is simply dummy text of the printing and</p>
    </footer>
  </body>
</html>